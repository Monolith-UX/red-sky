# Netlify detects Next.js and applies its runtime (@netlify/plugin-nextjs) on its own.
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "24"
  NEXT_TELEMETRY_DISABLED = "1"
